\documentclass[tikz, border=0]{standalone}
\usepackage{fontspec}
\usepackage{unicode-math}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\setmainfont{TeX Gyre Pagella}
\setmathfont{TeX Gyre Pagella Math}
\usepackage{luacode} % Pacote necessário para rodar Lua
\usetikzlibrary{calc,arrows.meta,patterns}

\definecolor{Red}{RGB}{ 159, 36, 48 }
\definecolor{Blue}{RGB}{ 103, 178, 255 }
\definecolor{LBlue}{RGB}{ 186, 220, 255 }
\definecolor{DBlue}{HTML}{132C53} 
\definecolor{gray}{HTML}{708090}
\definecolor{orange}{HTML}{ed9552}
\definecolor{orange2}{HTML}{e29d40}
\definecolor{PrintColor}{HTML}{000000}

\pgfmathdeclarefunction{V}{3}{%
        \pgfmathparse{ -( (#2/2)*(abs(#1)^2) + ((#3-#2)/4)*(abs(#1)^4) - (#3/6)*(abs(#1)^6))}%
    }

\tikzset{
    ball style/.style={circle, fill=gray, draw=white, line width=0.15em, inner sep=0.4em},
    noise arrow/.style={<->, >=stealth, thick, gray, shorten <= 2pt, shorten >= 2pt},
    drift arrow/.style={->, >=stealth, thick, line width=0.3em, gray, shorten <= 2pt, shorten >= 2pt},
    jump arrow/.style={->, >=stealth, dashed, very thick, gray, shorten <= 4pt, shorten >= 4pt},
    indicate arrow/.style={->, >=stealth, very thick, gray, shorten <= 4pt, shorten >= 4pt}
}

% Comando para desenhar a bolinha na posição exata x. 
% Argumentos: #1 = x_pos, #2 = Y_scale, #3 = a, #4 = b, #5 = nome_do_node
\newcommand{\DrawBall}[5]{
    \coordinate (pos) at ({22*(#1)/1.2}, {#2*V(#1,#3,#4)});
    \node[ball style] (#5) at (pos) {};
}
\newcommand{\DrawArrow}[5]{
    \draw[drift arrow] ({22*(#1)/1.2}, {#2*V(#1,#3,#4)}) -- ({22*(#1+#5)/1.2}, {#2*V(#1+#5,#3,#4)});
}

% Comando para desenhar as setinhas de flutuação acima da bolinha
\newcommand{\NoiseArrows}[1]{
    \draw[noise arrow] ([yshift=0.8em, xshift=-1.2em]#1.north) to[bend left=10] ([yshift=0.8em, xshift=1.2em]#1.north);
}

\newcommand{\BeginMacros}{
        \def\Lx{22}
        \def\Ly{12}

        \pgfmathsetmacro{\magP}{22/1.2}
        \pgfmathsetmacro{\magM}{-22/1.2}

        \useasboundingbox (-22.5,-14.5) rectangle (22.5,12.5);

        \coordinate (center) at (0,0);

        \coordinate (topC) at (0,\Ly);
        \coordinate (topR) at (\Lx,\Ly);
        \coordinate (topL) at (-\Lx,\Ly);
        \coordinate (topRm) at (0.5*\Lx,\Ly);
        \coordinate (topLm) at (-0.5*\Lx,\Ly);
        
        \coordinate (botC) at (0,-\Ly);
        \coordinate (botR) at (\Lx,-\Ly);
        \coordinate (botL) at (-\Lx,-\Ly);
        \coordinate (botRm) at (0.5*\Lx,-\Ly);
        \coordinate (botLm) at (-0.5*\Lx,-\Ly);

        \coordinate (leftC) at (-\Lx,0);
        \coordinate (leftTm) at (-\Lx,0.5*\Ly);
        \coordinate (leftBm) at (-\Lx,-0.5*\Ly);

        \draw[step=11/0.6, dashed, gray] (botL) grid (topR);
}

\newcommand{\EndMacros}{
    \node[PrintColor, anchor=north, yshift=-0.5em, font=\bfseries] at (botC) {Magnetização $\symbf{\phi}$};
    \node[PrintColor, anchor=north, yshift=-0.5em, font=\bfseries] at (\magM,-\Ly) {$\symbf{-1}$};
    \node[PrintColor, anchor=north, yshift=-0.5em, font=\bfseries] at (\magP,-\Ly) {$\symbf{+1}$};
    \node[PrintColor, anchor=south, yshift=0.5em, font=\bfseries] at (0,-\Ly) {$\symbf{0}$};

    \draw[gray, very thick] (\magM,-\Ly) -- (\magM,-\Ly+0.5);
    \draw[gray, very thick] (0,-\Ly) -- (0,-\Ly+0.5);
    \draw[gray, very thick] (\magP,-\Ly) -- (\magP,-\Ly+0.5);
    \node[PrintColor, very thick, anchor=south east] at (\magM,0) {$\symbf{V=0}$};
    \draw[PrintColor, thick] (botL) rectangle (topR);
}

\newcommand{\Legend}{
    \begin{scope}[shift={(\magP,0)}]
        \draw[noise arrow, yshift=1em] (-1.5,3) to[bend left=10] (1.5,3);
        \node[PrintColor, anchor=north, yshift=1em] at (0,3) {Ruído};
        \draw[drift arrow, yshift=1em] (-2,0) -- (2,0)
            node[PrintColor, midway, below, anchor=north] {Drift};
        \draw[jump arrow, yshift=1em] (-2,-3) -- (2,-3)
            node[PrintColor, midway, below, anchor=north] {Efetivo};
    \end{scope}
}

\begin{document}

\begin{tikzpicture}[x=1em, y=1em]
    \BeginMacros
    \draw[Blue, very thick, line width=0.3em] plot
        [domain=-1.2:1.2, samples=120]
            ({22*\x/1.2}, {50*V(\x,1,-1)});
    \draw[LBlue, very thick, line width=0.3em] plot
        [domain=-1.2:1.2, samples=120]
            ({22*\x/1.2}, {50*V(\x,-1,1)});
    \draw[Red, very thick, line width=0.3em] plot
        [domain=-1.2:1.2, samples=120]
            ({22*\x/1.2}, {120*V(\x,-0.5,2.5)});

    \EndMacros
    
\end{tikzpicture} 

\begin{tikzpicture}[x=1em, y=1em]
    \BeginMacros
    \node[PrintColor, anchor=north, yshift=-0.5em, font=\bfseries] at (topC) {Fase Ferromagnética};

    \draw[Blue, very thick, line width=0.3em] plot
        [domain=-1.2:1.2, samples=120]
            ({22*\x/1.2}, {50*V(\x,1,-1)});

    \DrawBall{-0.4}{50}{1}{-1}{ballL}
    \DrawArrow{-0.4}{50}{1}{-1}{-0.1}
    \DrawBall{ 0.4}{50}{1}{-1}{ballR}
    \DrawArrow{0.4}{50}{1}{-1}{0.1}
    \DrawBall{ 0}{50}{1}{-1}{ballC}
    \NoiseArrows{ballC}

    \draw[jump arrow] (ballL.south) to[out=-120, in=-60] (\magM,{50*V(-1,1,-1)});
    \draw[jump arrow] (ballR.south) to[out=-60, in=-120] (\magP,{50*V(1,1,-1)});

    \EndMacros
    \Legend
    
\end{tikzpicture}    
    

\begin{tikzpicture}[x=1em, y=1em]
    \BeginMacros
    \node[PrintColor, anchor=north, yshift=-0.5em, font=\bfseries] at (topC) {Fase mista};

    \draw[Red, very thick, line width=0.3em] plot
        [domain=-1.2:1.2, samples=120]
            ({22*\x/1.2}, {120*V(\x,-0.5,2.5)});

    \pgfmathsetmacro{\Min}{
        {120*V(-1,-0.5,2.5)}
    }

    \DrawBall{-0.45}{120}{-0.5}{2.5}{ballL}
    \DrawBall{ 0.45}{120}{-0.5}{2.5}{ballR}
    \NoiseArrows{ballR} 
    \NoiseArrows{ballL} 

    \draw[jump arrow] (ballL.north) to[out=60, in=90] ({0},{120*V(0,-0.5,2.5)});
    \draw[jump arrow] (ballL.north) to[out=120, in=90] (\magM,\Min);

    \DrawBall{-0.7}{120}{-0.5}{2.5}{ballL}
    \DrawArrow{-0.7}{120}{-0.5}{2.5}{-0.1}
    \DrawBall{ 0.7}{120}{-0.5}{2.5}{ballR}
    \DrawArrow{0.7}{120}{-0.5}{2.5}{0.1}
    
    \EndMacros
    \Legend
    
\end{tikzpicture}

\begin{tikzpicture}[x=1em, y=1em]
    
    \BeginMacros

    \pgfmathsetmacro{\Min}{
        {120*V(-1,-0.5,2.5)}
    }

    \node[PrintColor,font=\bfseries] (ferro) at (0,-0.5*\Ly) {Mínimos Ferromagnéticos};
    \node[PrintColor,font=\bfseries] (para) at (0,0.5*\Ly) {Mínimo Paramagnético};

    \draw[indicate arrow] (\magP,\Min) -- (ferro);
    \draw[indicate arrow] (\magM,\Min) -- (ferro);
    \draw[indicate arrow] (0,2) -- (para);

    
\end{tikzpicture}


\begin{tikzpicture}[x=1em, y=1em]
    \BeginMacros
    \node[PrintColor, anchor=north, yshift=-0.5em, font=\bfseries] at (topC) {Fase Paramagnética};

    \draw[LBlue, very thick, line width=0.3em] plot
        [domain=-1.2:1.2, samples=120]
            ({22*\x/1.2}, {50*V(\x,-1,1)});

    \DrawBall{-0.6}{50}{-1}{1}{ballL}
    \DrawArrow{-0.6}{50}{-1}{1}{0.1}
    \DrawBall{ 0.6}{50}{-1}{1}{ballR}
    \DrawArrow{ 0.6}{50}{-1}{1}{-0.1}
    \DrawBall{ 0}{50}{-1}{1}{ballC}    
    \NoiseArrows{ballC}

    \draw[jump arrow] (ballL.north) to[out=30, in=120] (-1,1);
    \draw[jump arrow] (ballR.north) to[out=150, in=60] ( 1,1);
    
    \EndMacros
    \Legend

\end{tikzpicture}

\end{document}